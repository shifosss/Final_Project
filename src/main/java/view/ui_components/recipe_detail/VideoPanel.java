package view.ui_components.recipe_detail;

import javax.swing.*;

/**
 * Contains the playable video.
 */
public class VideoPanel extends JPanel {
    public VideoPanel(JComponent videoPlayer) {
        add(videoPlayer);
    }
}
